- show_password = false
.ui.padded.grid
  .column
    %h1 Listing Users
    %table.ui.striped.blue.table
      %thead
        %tr
          %th ชื่อ - สกุล
          %th ชื่อผู้ใช้ (สำหรับการเข้าสู่ระบบ)
          - if show_password
            %th รหัสผ่าน
          %th สิทธิ์การเข้าใช้
          %th{colspan: "2"}
      %tbody
        - @users.each do |user|
          %tr
            %td= user.name
            %td= user.username
            - if show_password
              %td= user.password_digest
            %td= user.privilege
            %td= link_to content_tag(:i, "", class: "edit icon") + "Edit", edit_user_path(user)
            %td= link_to content_tag(:i, "", class: "trash icon") + "Delete", user, method: :delete, data: {confirm: "Are you sure to delete \"#{user.name}\""}
